\begin{definition}
  We say that \( n\in\mathbb{N} \) is composite iff
  \( n\neq 0 \) and there exist \( a,b\in\mathbb{N} \) such that
  \( a\neq 1\neq b \) and \( a\cdot b = n \).
\end{definition}

\begin{theorem}
  Suppose \( n \) is composite.
  Then \( n\cdot n \) is composite.
\end{theorem}
